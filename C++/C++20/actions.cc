// 

#include <vector>
#include <ranges>
#include <iostream>
#include <algorithm>

using namespace std;

int main()
{
    vector<int> data{4, 3, 4, 1, 8, 0, 8}; 
    // vector<int> result = data | actions::sort | actions::unique;

    // for(auto x : result)
    //     cout << x << ' ';
    // cout << endl;
    return 0;
}

